import React, { PropTypes, Component } from 'react';

class Resize extends Component {
    render = () => {
        return (
            <div>
                Hello World!
            </div>
        );
    };
}

export default Resize;
